{
  "name": "Homelab Detection Coverage",
  "versions": {
    "attack": "17",
    "navigator": "5.1.0",
    "layer": "4.5"
  },
  "domain": "enterprise-attack",
  "description": "Techniques with a verified custom Wazuh detection rule in this lab (phase-4-detection/detection-catalog.md). Updated as each technique's rule is written and confirmed to fire on a true positive.",
  "filters": {
    "platforms": ["Windows", "Linux"]
  },
  "sorting": 0,
  "layout": {
    "layout": "side",
    "aggregateFunction": "average",
    "showID": true,
    "showName": true
  },
  "hideDisabled": false,
  "techniques": [
    {
      "techniqueID": "T1110",
      "tactic": "credential-access",
      "color": "#3d8b40",
      "comment": "SSH brute force against dc-01 — rules 100010 (wrong password) + 100011 (non-existent user) in local_rules.xml, if_matched_sid/frequency/same_source_ip. Verified true positive live (not just wazuh-logtest); firewall-drop active response confirmed firing. Known gap: per-type thresholds, so mixing failure types across both rules evades detection (documented in detection-catalog.md).",
      "enabled": true,
      "score": 1
    },
    {
      "techniqueID": "T1484.001",
      "tactic": "defense-evasion",
      "color": "#3d8b40",
      "comment": "SYSVOL FIM escalation on dc-01 — rule 100020, scopes stock FIM rules 550/553/554 (modified/deleted/added) to /var/lib/samba/sysvol via the syscheck 'file' field, escalated to level 12. Verified true positive for add, modify (with full hash diff), and delete. No active response by design (alert-only; auto-reverting a file is riskier than blocking an IP). Untested gap: only exercised via local root on dc-01, not the more realistic remote-SMB write path (needs a second domain host).",
      "enabled": true,
      "score": 1
    },
    {
      "techniqueID": "T1558.003",
      "tactic": "credential-access",
      "color": "#3d8b40",
      "comment": "Kerberoasting against 3 intentionally-planted Kerberoastable service accounts (svc-sql/svc-backup/svc-web, weak passwords + SPNs, see phase-2-identity/known-weaknesses.md). Enabled Samba's structured auth_json_audit logging (off by default) to get per-TGS-REQ telemetry (Samba's equivalent of Windows Event ID 4769). Rules 100030 (base capture) + 100031 (if_matched_sid + same_field correlation on account, 3+ tickets/60s = level 12). Verified true positive live, AND verified the evasion: a single targeted ticket request produces zero new alerts, confirmed by alert count before/after, not just reasoned about.",
      "enabled": true,
      "score": 1
    },
    {
      "techniqueID": "T1110.001",
      "tactic": "credential-access",
      "color": "#3d8b40",
      "comment": "Kerberos pre-auth brute force against dc-01 — genuinely different detection surface from the sshd T1110 rules (same telemetry pipeline as Kerberoasting, eventId 4625 = real Windows Event ID). Rules 100040 (base) + 100041 (same_field correlation, 4+/60s). Paired with a real CUSTOM active-response script (disable-ad-account.py, since the stock disable-account command doesn't understand Samba AD accounts) that calls samba-tool to disable/re-enable the targeted account. Verified end-to-end: alert fired -> account's userAccountControl flipped 512->514 -> kinit with the CORRECT password failed with 'credentials have been revoked' -> automatic re-enable at the AR timeout confirmed too. Hard-excludes administrator/krbtgt/guest from automated action to prevent the response itself becoming a DoS vector.",
      "enabled": true,
      "score": 1
    },
    {
      "techniqueID": "T1053.003",
      "tactic": "persistence",
      "color": "#3d8b40",
      "comment": "Cron persistence on dc-01 — rule 100050, extends the FIM-escalation pattern proven on SYSVOL to /etc/cron.d, cron.{daily,hourly,weekly,monthly}, and /etc/crontab. Verified true positive by planting a disguised beacon cron job.",
      "enabled": true,
      "score": 1
    },
    {
      "techniqueID": "T1136.001",
      "tactic": "persistence",
      "color": "#3d8b40",
      "comment": "Local account creation on dc-01 (the simplest, protocol-independent backdoor once an attacker has root, regardless of how they got it) — rule 100051, FIM on /etc/passwd + /etc/shadow + /etc/sudoers. Also tagged T1098 since the same rule can't distinguish account creation from modifying an existing account's credentials -- verified test specifically exercised useradd (creation), correctly fired on both passwd and shadow writes.",
      "enabled": true,
      "score": 1
    },
    {
      "techniqueID": "T1562.001",
      "tactic": "defense-evasion",
      "color": "#3d8b40",
      "comment": "Arguably the highest-value rule in this catalog -- detects an attempt to disable the Wazuh agent, auditd, or samba-ad-dc itself, since every other rule here assumes the monitoring stays alive. Rule 100060, level 13 (highest in the catalog on purpose), pure escalation on the stock sudo rule (5402) with no new telemetry needed. Verified true positive via 'systemctl disable wazuh-agent' -- deliberately not 'stop', which would have killed the agent before it could report the alert about being killed. Documented real evasion: only catches the systemctl/service path, not SIGKILL or firewall-blocking the agent's outbound port; a real deployment needs manager-side disconnection monitoring as the actual backstop.",
      "enabled": true,
      "score": 1
    },
    {
      "techniqueID": "T1098.007",
      "tactic": "persistence",
      "color": "#e8a33d",
      "comment": "INVESTIGATED, NOT ACHIEVED — a real platform limitation, not a skipped step. Tried detecting Domain Admins group-membership changes via every plausible Samba dsdb audit-log facility (dsdb_audit, dsdb_json_audit, dsdb_group_audit, dsdb_group_json_audit, all combinations). Verified the underlying LDAP modification genuinely succeeded each time (samba-tool group listmembers confirmed) and verified the AUTHENTICATION half of the same operation WAS captured correctly by auth_json_audit -- proving the pipeline works and the gap is specific to directory-modification auditing in this Samba build (4.23.6), not a config mistake. A genuine, documented cost of the Samba-vs-Windows-Server DC pivot (docs/design-decisions.md) that the protocol-level equivalence argument doesn't cover. See detection-catalog.md for full methodology.",
      "enabled": true,
      "score": 0.3
    }
  ],
  "gradient": {
    "colors": ["#ff6666", "#ffe766", "#8ec843"],
    "minValue": 0,
    "maxValue": 1
  },
  "legendItems": [
    { "label": "Custom rule written + verified true positive", "color": "#3d8b40" },
    { "label": "Investigated — real platform limitation found, not achieved", "color": "#e8a33d" }
  ],
  "showTacticRowBackground": true,
  "tacticRowBackground": "#dddddd",
  "selectTechniquesAcrossTactics": true,
  "selectSubtechniquesWithParent": false
}
