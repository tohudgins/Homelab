---
# Applies to every managed host.
ansible_ssh_private_key_file: "~/.ssh/id_ed25519_homelab"
ansible_python_interpreter: /usr/bin/python3

# Lab-wide facts the roles template against (single source of truth for the
# addressing that docs/00-ip-plan.md documents by hand).
lab_domain: lab.internal
lab_realm: LAB.INTERNAL
ntp_server: 10.10.10.1          # rtr-01 CORP-side gateway = chrony source

networks:
  corp:    { cidr: "10.10.10.0/24", gw: "10.10.10.1" }
  dmz:     { cidr: "10.10.20.0/24", gw: "10.10.20.1" }
  soc:     { cidr: "10.10.30.0/24", gw: "10.10.30.1" }
  redteam: { cidr: "10.10.40.0/24", gw: "10.10.40.1" }

wazuh_manager_ip: 10.10.30.10   # siem-01
