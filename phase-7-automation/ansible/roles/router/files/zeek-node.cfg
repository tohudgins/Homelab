# Standalone Zeek on the CORP interface — sees all traffic to/from the AD servers
# (dc-01/fs-01/ws-01), including inter-segment attacks routed through rtr-01.
[zeek]
type=standalone
host=localhost
interface=enp26s0
