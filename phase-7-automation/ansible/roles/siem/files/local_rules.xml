<!-- Local rules -->
<!-- Homelab Phase 4 — custom detection engineering -->
<!-- Copyright (C) 2015, Wazuh Inc. -->

<!--
  T1110 — Brute Force (SSH)

  First attempt used a single if_matched_group="authentication_failed" correlator to aggregate across
  failure types (wrong password AND non-existent user) with one tighter threshold than the stock
  per-type correlators (5712/5719/5763, each frequency=8/120s independently). Verified via
  wazuh-logtest that if_matched_group never fires in this deployment — reproduced with a literal copy
  of the stock ruleset's own if_matched_group rule (40111, same pattern, only frequency changed) and
  it didn't fire either, while if_matched_sid (stock rule 5763) fired exactly as documented. Rather
  than chase what looks like a version-specific bug, pivoted to two explicit if_matched_sid rules
  below — same intent (tighter threshold + active response), proven mechanism.
-->
<group name="local,syslog,sshd,">

  <rule id="100010" level="12" frequency="4" timeframe="60" ignore="120">
    <if_matched_sid>5760</if_matched_sid>
    <same_source_ip />
    <description>sshd: brute force — $(srcip) had 4+ wrong-password failures in 60s.</description>
    <mitre>
      <id>T1110</id>
      <id>T1110.001</id>
    </mitre>
    <group>authentication_failures,attack,</group>
  </rule>

  <rule id="100011" level="12" frequency="4" timeframe="60" ignore="120">
    <if_matched_sid>5710</if_matched_sid>
    <same_source_ip />
    <description>sshd: brute force — $(srcip) had 4+ non-existent-user failures in 60s.</description>
    <mitre>
      <id>T1110</id>
      <id>T1110.001</id>
    </mitre>
    <group>authentication_failures,attack,</group>
  </rule>

  <!--
  Evasion follow-up: both key on same_source_ip and a single failure type, so (a) a spray that stays
  under 4/60s from any single source, or (b) an attacker who mixes wrong-password and invalid-user
  guesses to split their attempts across 100010 and 100011 without tripping either threshold alone,
  both evade this pair by design. Documented in detection-catalog.md as a known gap, not patched here.
  -->

</group>

<!--
  T1484.001 — Group Policy Modification (SYSVOL integrity)

  Default FIM (rules 550/553/554 — modified/deleted/added) treats every monitored path identically:
  level 5-7 regardless of WHERE the change happened. That undersells SYSVOL specifically — it holds
  GPOs and logon scripts that apply to every domain-joined host on next policy refresh/logon, so a
  single unauthorized write there has a blast radius no other path in this lab has. This rule doesn't
  replace the base FIM rules (they still fire, still carry the full hash/attribute record) — it adds
  a second, higher-severity alert scoped to that one path via the `file` dynamic field syscheck
  decodes into.
-->
<group name="local,syscheck,">

  <rule id="100020" level="12">
    <if_sid>550,553,554</if_sid>
    <field name="file" type="pcre2">^/var/lib/samba/sysvol</field>
    <description>SYSVOL integrity change (GPO/logon script) — $(file)</description>
    <mitre>
      <id>T1484.001</id>
    </mitre>
    <group>gpo_tampering,attack,</group>
  </rule>

  <!--
  No active response wired here on purpose — auto-reverting a file is a much riskier default action
  than blocking an IP (could clobber a legitimate GPO edit made seconds earlier), so this stays
  alert-only pending a real response playbook. See detection-catalog.md.
  -->

  <!--
  T1053.003 — Scheduled Task/Job: Cron (Persistence)

  Same escalation pattern as SYSVOL above, different path set: cron directories are where a compromised
  host most commonly gets a persistence mechanism planted (a scheduled beacon/implant re-download).
  Extended real-time FIM to cover them plus a handful of other classic Linux persistence/credential
  files (/etc/passwd, /etc/shadow, /etc/sudoers) in the same <directories> block.
  -->
  <rule id="100050" level="12">
    <if_sid>550,553,554</if_sid>
    <field name="file" type="pcre2">^/etc/cron\.(d|daily|hourly|weekly|monthly)/|^/etc/crontab$</field>
    <description>Cron persistence — $(file) added/modified/deleted.</description>
    <mitre>
      <id>T1053.003</id>
    </mitre>
    <group>persistence,attack,</group>
  </rule>

  <!--
  T1136.001 (Create Account: Local Account) / T1098 (Account Manipulation) — local account and
  credential-file tampering (host-level, distinct from the Samba/AD-level T1098 investigation above —
  this catches changes to the OS's OWN account files, relevant even though the primary identity plane
  here is AD, since dc-01 itself is still a Linux host with local accounts/sudoers that could be abused
  for persistence independent of AD entirely). Both MITRE IDs tagged deliberately, not sloppily: the rule
  can't distinguish creating a new local account (useradd writes both passwd+shadow) from modifying an
  existing one's password/shell (T1098) — same honest broad-not-narrow tradeoff as the rest of this FIM
  family. The verified test below specifically exercised account *creation*.
  -->
  <rule id="100051" level="12">
    <if_sid>550,553,554</if_sid>
    <field name="file" type="pcre2">^/etc/(passwd|shadow|sudoers)$</field>
    <description>Local account/credential file changed — $(file).</description>
    <mitre>
      <id>T1136.001</id>
      <id>T1098</id>
    </mitre>
    <group>persistence,attack,</group>
  </rule>

</group>

<!--
  T1558.003 — Kerberoasting

  Samba's default log level (0) logs nothing about individual Kerberos ticket requests. Enabled
  structured audit logging (`log level = 1 auth_audit:3 auth_json_audit:3` in smb.conf) which emits
  real per-request JSON to /var/log/samba/log.samba, including a "KDC Authorization" event per TGS-REQ
  with the requesting account and the target SPN — this is Samba's equivalent of Windows Event ID 4769.
  Wired as a `log_format=json` localfile; Wazuh's JSON parser silently skips the human-readable lines
  log.samba also writes, so no separate decoder was needed for that half.
-->
<group name="local,kerberos,">

  <rule id="100030" level="3">
    <decoded_as>json</decoded_as>
    <field name="type">^KDC Authorization$</field>
    <options>no_full_log</options>
    <description>Samba KDC: TGS-REQ for a service ticket.</description>
    <group>kerberos_tgs,</group>
  </rule>

  <!--
    frequency="2", not "3". Re-verifying Phase 5 (2026-08-24) caught the original
    frequency="3" missing a real 3-SPN roast: Wazuh's `frequency="N"` fires on the
    (N+1)-th match, so "3" needed FOUR TGS-REQs, but impacket-GetUserSPNs sends
    exactly THREE (one per SPN account), landing one short. "2" fires on the 3rd,
    which is what the "3+ service tickets" description always meant.

    Still keyed on `KDC Authorization.account`. Against this Samba DC a failing
    impacket roast logs all three TGS-REQs with account=null (NT_STATUS_NO_SUCH_USER
    — the account never resolves), but they all share the SAME value ("null"), so
    same_field still groups them; a successful roast from a real client shares the
    real account. Correlating on source IP would be conceptually cleaner, but
    extracting a bare srcip needs a child decoder that strips the json fields rule
    100030 depends on — see the note in local_decoder.xml.
  -->
  <rule id="100031" level="12" frequency="2" timeframe="60" ignore="120">
    <if_matched_sid>100030</if_matched_sid>
    <same_field>KDC Authorization.account</same_field>
    <description>Kerberoasting suspected — $(KDC Authorization.account) requested 3+ service tickets in 60s.</description>
    <mitre>
      <id>T1558.003</id>
    </mitre>
    <group>attack,</group>
  </rule>

  <!--
  Evasion: correlation counts TGS-REQ OCCURRENCES from one account, not DISTINCT SPNs — a targeted
  attacker who already knows the one or two SPNs worth cracking (rather than sweeping every SPN in the
  domain) stays under the 3-in-60s threshold and evades this rule entirely. That's a meaningful real-world
  case: a targeted Kerberoast against one juiciest-looking service account looks identical to a legit
  client requesting its one ticket. Not patched here (would need baselining normal per-account SPN request
  patterns, out of scope for a rule this lab can verify against real telemetry).
  -->

  <!--
    DECEPTION: AD honeytoken (svc-sqladmin) — a decoy service account with an attractive SPN
    (MSSQLSvc/sql-prod.lab.internal) and a strong, uncrackable password. NO legitimate process ever
    requests a ticket for it or authenticates as it, so ANY interaction is malicious by definition — a
    tripwire with ~zero false positives, which is why these fire at level 14 (above every other rule in
    this catalog). This is what closes the evasion above: the targeted single-SPN Kerberoast that slips
    under 100031's volume threshold still trips 100420 on the FIRST request, because the signal here is
    the SPN's identity, not the request count. Deception-backed Credential Access. Managed as code in the
    dc role (roles/dc: account + SPN) and here (the detections). See phase-4-detection/deception/.
  -->
  <rule id="100420" level="14">
    <if_sid>100030</if_sid>
    <field name="KDC Authorization.serviceDescription" type="pcre2">(?i)MSSQLSvc/sql-prod\.lab\.internal</field>
    <options>no_full_log</options>
    <description>HONEYTOKEN TRIPWIRE — TGS-REQ for decoy account svc-sqladmin by $(KDC Authorization.account) (Kerberoasting against a canary SPN).</description>
    <mitre>
      <id>T1558.003</id>
    </mitre>
    <group>deception,honeytoken,attack,credential_access,</group>
  </rule>

  <rule id="100421" level="14">
    <decoded_as>json</decoded_as>
    <field name="type">^Authentication$</field>
    <field name="Authentication.clientAccount" type="pcre2">(?i)^svc-sqladmin(@|$)</field>
    <options>no_full_log</options>
    <description>HONEYTOKEN TRIPWIRE — authentication attempt AS decoy account svc-sqladmin ($(Authentication.status)) — stolen/guessed canary credential in use.</description>
    <mitre>
      <id>T1078</id>
    </mitre>
    <group>deception,honeytoken,attack,credential_access,</group>
  </rule>

  <!--
    RANSOMWARE CANARY (T1486 Impact) — a decoy "finance records" directory on fs-01 under realtime FIM
    that no legitimate user ever touches (deployed by the fileserver role). Two signals:
      100430 (L12) — ANY change to a canary file. It's bait, so a single touch is already an incident
                     (same tripwire logic as the AD honeytoken above), scoped by the `file` path.
      100431 (L13) — a BURST of canary changes (8+ in 20s from one agent). That cadence — many files
                     rewritten in seconds — is the fingerprint of ransomware encrypting a share, as opposed
                     to a human editing one file. Fills the Impact tactic. See phase-4-detection/deception/.
  -->
  <rule id="100430" level="12">
    <if_sid>550,553,554</if_sid>
    <field name="file" type="pcre2">^/srv/finance-records/</field>
    <description>Ransomware canary tampering — decoy file $(file) was changed (nobody should ever touch this).</description>
    <mitre>
      <id>T1486</id>
    </mitre>
    <group>deception,ransomware_canary,attack,impact,</group>
  </rule>

  <rule id="100431" level="13" frequency="8" timeframe="20">
    <if_matched_sid>100430</if_matched_sid>
    <same_agent />
    <description>Ransomware behavior — 8+ canary files changed in 20s (mass-encryption burst).</description>
    <mitre>
      <id>T1486</id>
    </mitre>
    <group>ransomware_canary,attack,impact,</group>
  </rule>

</group>

<!--
  T1110.001 — Brute Force: Password Guessing (Kerberos pre-authentication, distinct from the sshd-based
  100010/100011 above)

  Same ATT&CK ID, genuinely different detection surface: an attacker guessing AD account passwords via
  Kerberos (kinit, or any AD-aware tool) never touches sshd at all, so 100010/100011 are blind to this
  vector entirely. Reuses the same auth_json_audit telemetry wired up for Kerberoasting above — Samba
  emits an eventId 4625 (matches real Windows Event ID 4625) per failed pre-auth attempt.
-->
<group name="local,kerberos,">

  <rule id="100040" level="5">
    <decoded_as>json</decoded_as>
    <field name="type">^Authentication$</field>
    <field name="Authentication.eventId">^4625$</field>
    <field name="Authentication.serviceDescription">^Kerberos KDC$</field>
    <options>no_full_log</options>
    <description>Samba KDC: Kerberos pre-authentication failed for $(Authentication.clientAccount).</description>
    <mitre>
      <id>T1110.001</id>
    </mitre>
    <group>authentication_failed,</group>
  </rule>

  <rule id="100041" level="12" frequency="4" timeframe="60" ignore="120">
    <if_matched_sid>100040</if_matched_sid>
    <same_field>Authentication.clientAccount</same_field>
    <description>Kerberos brute force — $(Authentication.clientAccount) had 4+ pre-auth failures in 60s.</description>
    <mitre>
      <id>T1110</id>
      <id>T1110.001</id>
    </mitre>
    <group>authentication_failures,attack,</group>
  </rule>

  <!--
  Active response: disables the targeted AD account via samba-tool rather than blocking a source IP.
  IP-blocking (used for the sshd rules above) doesn't fit here — a Kerberos brute force can originate
  from any domain member, and blocking one source IP does nothing if the attacker just tries from
  another host. Disabling the *account* stops the attack regardless of source. Custom script since the
  stock `disable-account` active response only understands local system accounts, not Samba AD accounts.
  See disable-ad-account.py (mirrored in this folder, deployed without the extension per Wazuh's
  active-response naming convention) and detection-catalog.md for the real risk this creates: an
  attacker who knows this rule exists can weaponize it into a DoS by deliberately failing Kerberos auth
  as a real, legitimate, high-value account (helpdesk lead, an admin) to get it locked out.
  -->

</group>

<!--
  T1110.003 — Brute Force: Password Spraying (SMB/NTLM, the horizontal counterpart to 100040/100041)

  100041 above keys on same_field Authentication.clientAccount — it fires only when ONE account fails
  4+ times (a vertical brute force). A password SPRAY is the opposite shape: ONE password tried once
  against MANY accounts, so no single account crosses the threshold and 100041 never fires. Confirmed
  live: `nxc smb 10.10.10.10 -u <all-users> -p Summer2026` produced eight distinct-account SMB results
  (seven failures + the one weak-password reuse it found, svc-sql) and 100041 stayed silent — a real,
  reproducible detection gap.

  Two things had to be added to close it:
  1. Telemetry. SMB session-setup auth on a Samba AD DC is handled by *smbd*, which logs to
     /var/log/samba/log.smbd — NOT log.samba (the AD DC process, all the Kerberoast/brute rules ever
     saw). SMB failures were therefore invisible to Wazuh. Added log.smbd as a second json localfile on
     dc-01. Samba emits the same eventId 4625 for a failed SMB NTLM logon, serviceDescription "SMB2".
  2. Correlation on the burst, NOT on the account. 100401 deliberately omits same_field, so it counts
     failed SMB logons across the whole domain in the window — which is exactly the spray signature.

  Source-IP attribution would be conceptually cleaner (a spray is "one host sweeping accounts"), but
  Authentication.remoteAddress carries a per-connection source PORT ("ipv4:10.10.40.119:44274") so it
  can't group, and extracting a bare srcip needs a child decoder that strips the parent json fields —
  the same documented dead-end noted on the Kerberoast rule. Frequency-of-SMB-failures is the robust
  signal in a single-DC lab; a production tuning would baseline normal failure volume and add srcip.
-->
<group name="local,authentication_failures,">

  <rule id="100400" level="5">
    <decoded_as>json</decoded_as>
    <field name="type">^Authentication$</field>
    <field name="Authentication.eventId">^4625$</field>
    <field name="Authentication.serviceDescription">^SMB2$</field>
    <options>no_full_log</options>
    <description>Samba SMB: failed NTLM logon for $(Authentication.clientAccount) from $(Authentication.remoteAddress).</description>
    <mitre>
      <id>T1110.001</id>
    </mitre>
    <group>authentication_failed,</group>
  </rule>

  <!--
    frequency="4" fires on the 5th match (Wazuh's N+1 quirk, as documented on 100031). A spray sweeps
    many accounts, so 5+ failed SMB logons in the window is the threshold. No same_field: that is the
    whole point — the failures are across DIFFERENT accounts, which is why the per-account brute rule
    (100041) can never see it.
  -->
  <rule id="100401" level="12" frequency="4" timeframe="120" ignore="120">
    <if_matched_sid>100400</if_matched_sid>
    <description>Password spray suspected — 5+ failed SMB logons across the domain in 120s (single-source credential attack; 100041's per-account brute rule would miss this horizontal pattern).</description>
    <mitre>
      <id>T1110</id>
      <id>T1110.003</id>
    </mitre>
    <group>authentication_failures,attack,</group>
  </rule>

</group>

<!--
  T1562.001 — Impair Defenses: Disable or Modify Tools

  Arguably the single highest-value rule in this catalog: an attacker who disables the monitoring itself
  makes every other rule above moot. Chains off the stock sudo rule (5402, any successful sudo-to-root)
  rather than needing new telemetry — this is a pure escalation on top of what's already flowing.
-->
<group name="local,attack,">

  <rule id="100060" level="13">
    <if_sid>5402</if_sid>
    <field name="command" type="pcre2">systemctl\s+(stop|disable|mask)\s+\S*(wazuh-agent|auditd|samba-ad-dc)|service\s+(wazuh-agent|auditd|samba-ad-dc)\s+stop</field>
    <description>Security tooling disruption attempt — $(command)</description>
    <mitre>
      <id>T1562.001</id>
    </mitre>
    <group>defense_evasion,attack,</group>
  </rule>

  <!--
  Evasion: only catches the OBVIOUS path (systemctl/service stopping the named unit). A more careful
  attacker sends SIGSTOP/SIGKILL directly to the wazuh-agent process, or blocks 1514/tcp outbound at the
  host firewall, or corrupts the agent's own config — none of which touch this rule at all, and the
  irony is real: an agent that's been killed can't alert that it was killed. A production deployment
  would pair this with manager-side "agent went silent unexpectedly" monitoring (Wazuh's own
  disconnection alerting), which doesn't depend on the compromised agent reporting anything at all.
  -->

</group>

<!--
  T1547.001 — Registry Run Keys (ws-01, closing a real gap in the stock Sysmon ruleset)

  Wazuh's stock Sysmon EID 13 rules (92300-92304 in 0860-sysmon_id_13.xml) only escalate to a visible
  alert for specific narrow patterns: reg.exe usage (92302), a suspicious file extension in the value
  (92301), or a known remote-access-tool signature (92303). The base classification rule (92300, any
  write to a Run/RunOnce key) is level 0 — matched internally, never alerted.

  Verified this is a real gap, not a theoretical one: writing the exact same Run key via reg.exe
  triggered 92302 correctly; writing it via PowerShell's New-ItemProperty — arguably the more common
  real-world persistence method, and one specifically called out because it's what a
  living-off-the-land/fileless technique would actually use instead of the much-more-signature'd
  reg.exe — produced zero alerts, even though 92300 matched it silently. This rule closes that gap by
  escalating the base rule generically, regardless of which tool wrote the key.
-->
<group name="local,attack,">

  <rule id="100070" level="10">
    <if_sid>92300</if_sid>
    <description>Registry Run key persistence — $(win.eventdata.image) set $(win.eventdata.targetObject)</description>
    <mitre>
      <id>T1547.001</id>
    </mitre>
    <group>persistence,attack,</group>
  </rule>

  <!--
  Deliberately a level below the stock rules' specific-pattern escalations (12): this is a broad
  catch-all, so it will fire for legitimate app installers adding real autostart entries too, not just
  attacks. That tradeoff (real coverage vs. real noise) is the honest cost of closing this gap — see
  detection-catalog.md.
  -->

</group>

<!--
  Phase 5 (offense-in-context): DCSync detection.

  DCSync = an attacker with the DS-Replication-Get-Changes[-All] rights (here deliberately granted to
  svc-backup, the account the fs-01 share bait leaks) asks the DC to REPLICATE account secrets to them
  via the MS-DRSR DsGetNCChanges call, dumping password hashes without ever touching disk on the DC.

  On this Samba DC the impacket implementation actually fails (it sends a prefixMap Samba can't decode —
  see BloodHound notes), but that is irrelevant to detection: the malicious DsGetNCChanges request still
  reaches the DC's replication handler, and Samba logs it (source4/rpc_server/drsuapi/getncchanges.c).
  dc-01 forwards these plain-text lines via a dedicated syslog localfile (the json localfile that carries
  the auth audit silently drops non-JSON lines).

  Single-DC assumption: with only one DC there is NO legitimate DC-to-DC replication, so a GetNCChanges
  request is unambiguously an attack -> level 12. In a multi-DC domain this rule would need to also
  exclude requests whose source is a real DC (by IP / machine account). Also: this fires on the impacket
  error path; to catch a *successful* replication from a non-buggy client, raise `log level drsuapi:5`
  in smb.conf so every DsGetNCChanges is logged, then match the same handler string.
-->
<group name="local,dcsync,">

  <rule id="100080" level="12">
    <match>dcesrv_drsuapi_DsGetNCChanges</match>
    <description>DCSync suspected — AD replication (DsGetNCChanges) invoked on the DC; no legitimate DC-to-DC replication exists in this single-DC domain, indicating credential-replication abuse.</description>
    <mitre>
      <id>T1003.006</id>
    </mitre>
    <group>dcsync,credential_access,attack,</group>
  </rule>

</group>

<!--
  Phase 5: fs-01 weak-share credential-theft detection (closes the T1552.001 blind spot).
  FIM (syscheck) catches file *changes*, never *reads* — so reading the planted credential produced no
  alert. Samba `full_audit` VFS on the [public] share logs every openat to smbd_audit (journald), decoded
  above into smb_user/srcip/smb_share/smb_op/smb_path. This rule fires on an openat of the bait file
  map-backup-share.ps1 (which embeds svc-backup/Backup2026) — i.e. the exact credential-theft action.
-->
<group name="local,samba,">

  <rule id="100090" level="12">
    <decoded_as>smbd</decoded_as>
    <field name="smb_path" type="pcre2">map-backup-share\.ps1$</field>
    <description>Credential theft — planted svc-backup credential file read on the fs-01 weak share by $(smb_user) from $(srcip).</description>
    <mitre>
      <id>T1552.001</id>
    </mitre>
    <group>credential_access,attack,</group>
  </rule>

</group>

<!--
  Discovery tactic (ws-01) — closing ATT&CK-mapping gaps found by running an Atomic Red Team
  Discovery battery (T1016/T1049/T1082/T1057/T1087.001/T1069.001/T1518.001) and diffing what the
  stock ruleset actually maps. Findings that drove these rules:

    * Stock rule 92031 ("Discovery activity executed", T1087) matches ONLY net.exe/net1.exe — so
      `net localgroup`/`net group` (which is Permission Groups Discovery) is caught but MIS-tagged as
      T1087 Account Discovery rather than T1069.001. 100106 refines that mapping.
    * ipconfig / netsh / arp / route / nbtstat / nslookup (T1016), and netstat (T1049), have NO stock
      rule at all in this ruleset — they generate Sysmon EID1 events that decode fine but never map to
      their technique. 100100–100103 add that coverage (native binary + PowerShell cmdlet variants).
    * Security Software Discovery (T1518.001) — an attacker enumerating Sysmon/Defender/EDR before
      acting — also has no stock detection. 100104/100105 add it, and deliberately at a HIGHER level
      (6) than the rest: "what's watching me?" is a much stronger pre-attack signal than benign host
      recon, and far less common in normal admin activity.

  Severity model: pure host/network discovery (T1016/T1049/T1069.001) stays LOW (level 3) — these run
  constantly in legitimate admin/logon activity, so they are tagged-and-queryable for hunting and for
  the ATT&CK Navigator coverage layer, not paged on. Security-software discovery is level 6.

  Telemetry: native binaries come through Sysmon EID1 (chain <if_group>sysmon_event1</if_group>,
  match win.eventdata.originalFileName / commandLine); PowerShell cmdlet equivalents come through
  Script Block Logging EID 4104 (chain <if_sid>91802</if_sid>, match win.eventdata.scriptBlockText) —
  the same two sources the stock 92xxx/91xxx discovery rules use.

  Known limitation (NOT a rule below): the ideal actionable layer is a correlation rule that fires
  HIGH when many DISTINCT discovery commands hit one host in a short window (a host-enumeration sweep,
  vs. a single benign ipconfig). That needs cross-rule correlation via <if_matched_group>, which is
  non-functional in this Wazuh build — the same limitation already documented and worked around for
  the T1110 sshd rules (100010/100011). <if_matched_sid> works but takes a single SID, which can't
  span the whole discovery family, so the sweep-correlation is deferred rather than shipped broken.
-->
<group name="local,windows,sysmon,discovery,">

  <!-- T1016 — System Network Configuration Discovery (native binaries).
       Level 4 (not 3) on purpose: when these run via `cmd /c` the event ALSO matches stock rule
       92032 (L3, generic "Suspicious cmd shell execution", tagged T1087/T1059.003). Wazuh reports one
       rule per event, and on a level TIE the stock rule won — so a level-3 version of this rule was
       silently shadowed and the event kept the imprecise T1087 tag. L4 wins the event so it carries
       the correct T1016 mapping regardless of whether the parent is cmd.exe or a direct spawn.
       (Verified by exercising: at L3 this never appeared; at L4 it fires. Same one-alert-per-event
       precedence that makes the L6 security-software rules below always win.) -->
  <rule id="100100" level="4">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)(ipconfig|arp|route|nbtstat|nslookup|netsh)\.exe</field>
    <options>no_full_log</options>
    <description>Network configuration discovery — $(win.eventdata.originalFileName) on $(win.eventdata.parentImage)</description>
    <mitre>
      <id>T1016</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1016 — PowerShell cmdlet variant (Script Block Logging) -->
  <rule id="100101" level="3">
    <if_sid>91802</if_sid>
    <field name="win.eventdata.scriptBlockText" type="pcre2">(?i)Get-Net(IPConfiguration|IPAddress|Route|Neighbor|Adapter)\b|Get-DnsClientServerAddress</field>
    <options>no_full_log</options>
    <description>Network configuration discovery via PowerShell — $(win.eventdata.scriptBlockText)</description>
    <mitre>
      <id>T1016</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1049 — System Network Connections Discovery (netstat). Level 4 for the same 92032 tie-break
       reason documented on 100100 above. -->
  <rule id="100102" level="4">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)netstat\.exe</field>
    <options>no_full_log</options>
    <description>Network connections discovery — netstat on $(win.eventdata.parentImage)</description>
    <mitre>
      <id>T1049</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1049 — PowerShell cmdlet variant -->
  <rule id="100103" level="3">
    <if_sid>91802</if_sid>
    <field name="win.eventdata.scriptBlockText" type="pcre2">(?i)Get-NetTCPConnection\b|Get-NetUDPEndpoint\b</field>
    <options>no_full_log</options>
    <description>Network connections discovery via PowerShell — $(win.eventdata.scriptBlockText)</description>
    <mitre>
      <id>T1049</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1518.001 — Security Software Discovery (native: sc/reg/tasklist/net querying AV/EDR/Sysmon). Level 6. -->
  <rule id="100104" level="6">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)(sc|reg|tasklist|net|net1|sc\.exe)\b.{0,80}(sysmon|windefend|msmpeng|mssense|\bsense\b|securityhealth|mcafee|crowdstrike|falcon|carbonblack|cylance|sophos|symantec|windows\s*defender)</field>
    <options>no_full_log</options>
    <description>Security software discovery — enumerating AV/EDR/Sysmon: $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1518.001</id>
    </mitre>
    <group>discovery,recon,defense_evasion,</group>
  </rule>

  <!-- T1518.001 — PowerShell cmdlet variant (Defender/Sysmon enumeration). Level 6. -->
  <rule id="100105" level="6">
    <if_sid>91802</if_sid>
    <field name="win.eventdata.scriptBlockText" type="pcre2">(?i)Get-Mp(ComputerStatus|Preference|Threat|ThreatDetection)\b|Get-(Service|Process)\b.{0,60}(Sysmon|WinDefend|MsMpEng|Sense)</field>
    <options>no_full_log</options>
    <description>Security software discovery via PowerShell — $(win.eventdata.scriptBlockText)</description>
    <mitre>
      <id>T1518.001</id>
    </mitre>
    <group>discovery,recon,defense_evasion,</group>
  </rule>

  <!-- T1069.001 — Permission Groups Discovery: Local. Refines stock 92031 (net.exe, mis-tagged T1087)
       to the correct technique when the net command is specifically local group enumeration. -->
  <rule id="100106" level="3">
    <if_sid>92031</if_sid>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)\blocalgroup\b</field>
    <options>no_full_log</options>
    <description>Local permission-group discovery — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1069.001</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1069.001 — PowerShell cmdlet variant -->
  <rule id="100107" level="3">
    <if_sid>91802</if_sid>
    <field name="win.eventdata.scriptBlockText" type="pcre2">(?i)Get-LocalGroup(Member)?\b</field>
    <options>no_full_log</options>
    <description>Local permission-group discovery via PowerShell — $(win.eventdata.scriptBlockText)</description>
    <mitre>
      <id>T1069.001</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

</group>

<!--
  Discovery tactic — DOMAIN-scoped variants (ws-01, 2026-08-27, batch 2).

  Extends the local-discovery batch above to the AD-facing commands, found by running the domain
  variants against dc-01 and diffing the mapping. Two problems these fix:

    * `net group` (with or without /domain) queries the DC for GLOBAL groups = T1069.002 (Domain), while
      `net localgroup` is the LOCAL machine = T1069.001. Rule 100106 above originally matched both and
      tagged everything .001; it's now tightened to `localgroup` only, and 100109 here owns the domain
      case. (net.exe vs net1.exe: `net group` re-execs as net1.exe, so both originalFileNames appear.)
    * `nltest /dclist` (Remote System Discovery, T1018) fired no specific rule, and `nltest
      /domain_trusts` (Domain Trust Discovery, T1482 — a real AD attack-path recon step) was mis-tagged
      T1087 by the generic cmd rule. 100110/100111 map them correctly.

  All level 4 so they win the one-rule-per-event tie over the generic stock discovery rules (92031/92036,
  L3) and over the local-scope custom rules — see the precedence note on 100100. Domain-wide enumeration
  is also a genuinely stronger signal than local host recon, so the higher level is warranted on its own.
-->
<group name="local,windows,sysmon,discovery,">

  <!-- T1087.002 — Domain Account Discovery (net user /domain) -->
  <rule id="100108" level="4">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)net1?\.exe</field>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)\buser\b.{0,40}/domain</field>
    <options>no_full_log</options>
    <description>Domain account discovery — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1087.002</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1069.002 — Domain Groups Discovery (net group; \bgroup\b excludes "localgroup") -->
  <rule id="100109" level="4">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)net1?\.exe</field>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)\bgroup\b</field>
    <options>no_full_log</options>
    <description>Domain group discovery — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1069.002</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1018 — Remote System Discovery (nltest dclist / dsgetdc) -->
  <!-- Two quirks make this the trickiest of the batch: (1) nltest's PE OriginalFileName is "nltestrk.exe"
       (Resource Kit heritage), not "nltest.exe", so an originalFileName match silently misses it — key on
       the distinctive /dclist|/dsgetdc switch in the commandLine instead. (2) the direct nltest.exe
       /dclist process event is logged inconsistently here, but the `cmd /c "nltest /dclist…"` wrapper
       event reliably carries the switch — so this matches commandLine on ANY sysmon_event1 process, and is
       level 5 to win the one-rule-per-event tie over the stock cmd rule 92004 (L4) that also matches it. -->
  <rule id="100110" level="5">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)/(dclist|dsgetdc)</field>
    <options>no_full_log</options>
    <description>Remote system / DC discovery — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1018</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1482 — Domain Trust Discovery (nltest domain_trusts / trusted_domains) -->
  <!-- Broadened 2026-08-31 after the purple-team run: the nltest-only regex missed
       the dsquery/adfind variant (T1482 atomic test 1), which enumerates trusts via
       an LDAP filter on objectClass=trustedDomain. Added `trustedDomain` to cover
       dsquery `-filter "(objectClass=trustedDomain)"` and adfind. (PowerView
       Get-DomainTrust is PowerShell — a scriptBlockText rule, tracked separately.) -->
  <rule id="100111" level="4">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)(domain_trusts|trusted_domains|trustedDomain)</field>
    <options>no_full_log</options>
    <description>Domain trust discovery — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1482</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1087.002 / T1018 — PowerShell domain enumeration via ADSI or the AD module (no RSAT needed for
       ADSI/DirectorySearcher — the common living-off-the-land domain-enum path) -->
  <rule id="100112" level="4">
    <if_sid>91802</if_sid>
    <field name="win.eventdata.scriptBlockText" type="pcre2">(?i)adsisearcher|DirectorySearcher|System\.DirectoryServices|Get-AD(User|Computer|Group|GroupMember|Domain|Trust|DomainController)\b</field>
    <options>no_full_log</options>
    <description>Domain enumeration via PowerShell — $(win.eventdata.scriptBlockText)</description>
    <mitre>
      <id>T1087.002</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

  <!-- T1201 — Password Policy Discovery (net accounts). Level 4 to win the one-rule-per-event tie over
       stock 92031/92036 (L3), same reason as the T1016/T1049 rules above. -->
  <rule id="100113" level="4">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)net1?\.exe</field>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)\baccounts\b</field>
    <options>no_full_log</options>
    <description>Password policy discovery — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1201</id>
    </mitre>
    <group>discovery,recon,</group>
  </rule>

</group>

<!--
  T1218 — System Binary Proxy Execution (LOLBins) on ws-01 (2026-08-27, batch 3).

  Found by running an ART T1218 battery (mshta / regsvr32 / rundll32). Findings that drove these:
    * The LOLBins that execute (regsvr32 with a COM scriptlet = "Squiblydoo", rundll32) are detected only
      by the generic stock cmd rule 92032 and MIS-tagged T1087 — nothing maps them to T1218.
    * NOT a VBScript-deprecation problem: VBScript is Installed and functional here (cscript runs a .vbs,
      `mshta vbscript:close` exits 0). ART's mshta/rundll32-vbscript tests error at the harness `.Start()`
      (Access denied) — most likely Defender real-time protection killing the malicious child-spawn chain
      the payload builds — but the LOLBin process still launches with its telltale command line, so these
      rules are verified by invoking the binaries directly with a representative command line.

  High-signal patterns only (script monikers / scriptlets / INF-exec / HTA), deliberately NOT the bare
  binaries (rundll32 in particular runs constantly for legitimate reasons — plain Control_RunDLL is not
  matched). Level 4 to win the one-rule-per-event tie over the generic stock rules, per the precedence
  note earlier in this file.
-->
<group name="local,windows,sysmon,defense_evasion,">

  <!-- T1218.010 — Regsvr32 (Squiblydoo: /i: with a COM scriptlet / scrobj.dll) -->
  <rule id="100114" level="4">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)regsvr32\.exe</field>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)(scrobj\.dll|/i:)</field>
    <options>no_full_log</options>
    <description>Regsvr32 proxy execution (scriptlet/Squiblydoo) — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1218.010</id>
    </mitre>
    <group>defense_evasion,lolbin,attack,</group>
  </rule>

  <!-- T1218.011 — Rundll32 abuse: script monikers or INF-execution LOLBAS (not plain Control_RunDLL) -->
  <rule id="100115" level="4">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)rundll32.{0,80}(javascript:|vbscript:|RunHTMLApplication|LaunchINFSection|mshtml)</field>
    <options>no_full_log</options>
    <description>Rundll32 proxy execution (script moniker / INF LOLBAS) — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1218.011</id>
    </mitre>
    <group>defense_evasion,lolbin,attack,</group>
  </rule>

  <!-- T1218.005 — Mshta abuse: vbscript:/javascript: monikers, remote HTA, or a .hta payload -->
  <rule id="100116" level="4">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.originalFileName" type="pcre2">(?i)mshta\.exe</field>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)(vbscript:|javascript:|https?:|\.hta\b)</field>
    <options>no_full_log</options>
    <description>Mshta proxy execution (script moniker / HTA) — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1218.005</id>
    </mitre>
    <group>defense_evasion,lolbin,attack,</group>
  </rule>

</group>

<!--
  T1548.002 — Abuse Elevation Control Mechanism: Bypass UAC (ws-01, 2026-08-27, batch 4).

  Ran the classic registry-hijack UAC bypasses (fodhelper / eventvwr / sdclt / disable-via-reg) and diffed
  coverage. Stock rule 92304 (level 6) catches `Classes\(Folder|ms-settings)\shell\open\command` hijacks,
  and 92306 escalates to L12 *only when the writer is cmd/powershell*. Two real gaps:

    * **Event Viewer bypass has NO coverage.** 92304's regex is literally `(FOLDER|MS-SETTINGS)` — the
      `mscfile\shell\open\command` hijack the eventvwr bypass uses isn't in it, so the single most classic
      UAC bypass sails straight through (confirmed: the mscfile write produced zero alerts). 100117 closes
      it. This is the mirror image of the T1547.001 Run-key gap already documented in this catalog — a
      stock rule enumerating specific subkeys and missing one.
    * **Disabling UAC via the policy registry** (EnableLUA / ConsentPromptBehaviorAdmin) was detected only
      as T1027/T1112 (reg.exe obfuscation), never mapped to T1548.002. 100118 maps it.
-->
<group name="local,windows,sysmon,privilege_escalation,">

  <!-- T1548.002 — Event Viewer UAC bypass (mscfile hijack) — the subkey stock 92304 forgot -->
  <rule id="100117" level="12">
    <if_group>sysmon_event_13</if_group>
    <field name="win.eventdata.targetObject" type="pcre2">(?i)CLASSES\\\\MSCFILE\\\\SHELL\\\\OPEN\\\\COMMAND</field>
    <options>no_full_log</options>
    <description>UAC bypass via mscfile hijack (Event Viewer auto-elevate) — $(win.eventdata.targetObject)</description>
    <mitre>
      <id>T1548.002</id>
      <id>T1112</id>
    </mitre>
    <group>privilege_escalation,defense_evasion,attack,</group>
  </rule>

  <!-- T1548.002 — Disable UAC via policy registry (EnableLUA / ConsentPromptBehaviorAdmin) -->
  <rule id="100118" level="10">
    <if_group>sysmon_event_13</if_group>
    <field name="win.eventdata.targetObject" type="pcre2">(?i)CURRENTVERSION\\\\POLICIES\\\\SYSTEM\\\\(ENABLELUA|CONSENTPROMPTBEHAVIORADMIN|FILTERADMINISTRATORTOKEN)$</field>
    <options>no_full_log</options>
    <description>UAC weakened via policy registry — $(win.eventdata.targetObject) set to $(win.eventdata.details)</description>
    <mitre>
      <id>T1548.002</id>
      <id>T1112</id>
    </mitre>
    <group>privilege_escalation,defense_evasion,attack,</group>
  </rule>

</group>

<!--
  T1021.002 — Remote Services: SMB / Windows Admin Shares (ws-01, 2026-08-27, batch 5).

  Ran admin-share access from ws-01 to dc-01 (net use / New-SmbMapping to C$/ADMIN$/IPC$). Accessing a
  remote host's ADMIN$ or drive-letter$ share is a classic lateral-movement / remote-exec precursor, but
  stock only fired the generic net.exe rule 92036 (mistagged T1059.003/T1574.001) — nothing mapped it to
  T1021.002. (This lab is all-Linux except ws-01, so Windows→Windows PsExec/WinRM has no target; SMB
  admin-share access to the DC is the topology-appropriate lateral-movement surface.)

  IPC$ is deliberately excluded from the match — every normal SMB session opens IPC$, so it's pure noise;
  the regex `\\HOST\(ADMIN|<single-drive-letter>)$` matches C$/D$/ADMIN$ but not the 3-letter IPC$.
  Level 5 to win the one-rule-per-event tie over the stock net.exe (92036, L3) and cmd (92004, L4) rules.
-->
<group name="local,windows,sysmon,lateral_movement,">

  <!-- T1021.002 — admin-share UNC in a command line (net use / copy / dir \\host\C$ or \ADMIN$) -->
  <rule id="100119" level="5">
    <if_group>sysmon_event1</if_group>
    <field name="win.eventdata.commandLine" type="pcre2">(?i)\\\\(ADMIN|[A-Za-z])\$</field>
    <options>no_full_log</options>
    <description>Admin-share access (possible lateral movement) — $(win.eventdata.commandLine)</description>
    <mitre>
      <id>T1021.002</id>
    </mitre>
    <group>lateral_movement,attack,</group>
  </rule>

  <!-- T1021.002 — PowerShell remote share mapping (New-SmbMapping / New-PSDrive to a UNC) -->
  <rule id="100120" level="4">
    <if_sid>91802</if_sid>
    <field name="win.eventdata.scriptBlockText" type="pcre2">(?i)New-SmbMapping\b|New-PSDrive\b.{0,80}\\\\</field>
    <options>no_full_log</options>
    <description>Remote share mapping via PowerShell (possible lateral movement) — $(win.eventdata.scriptBlockText)</description>
    <mitre>
      <id>T1021.002</id>
    </mitre>
    <group>lateral_movement,attack,</group>
  </rule>

</group>

<!--
  ============================================================================
  Linux endpoint — C2 implant / malware execution from a world-writable path.
  Closes the endpoint blind spot the Phase 8 Sliver C2 hunt exposed: an
  in-memory beacon executed from /tmp was invisible to the host agent while the
  network sensor caught it. The fs-01 `susp-exec-path` command monitor (fileserver
  role) walks /proc/*/exe every 30s and emits `susp-exec path=<p> pid=<n>` for any
  process backed by a binary in /tmp, /dev/shm, /var/tmp, or deleted-while-running.
  A process executing from a world-writable directory is a strong T1204/T1059
  signal — legitimate services run from /usr, /bin, /opt, not /tmp.
  ============================================================================
-->
<group name="local,linux,c2,">

  <!-- Child of rule 530 (Wazuh process/command monitoring): a full_command
       localfile's output arrives as `ossec: output: '<alias>': <line>`, decoded
       by the ossec decoder and parented to 530 at level 0. Match its content. -->
  <rule id="100200" level="12">
    <if_sid>530</if_sid>
    <match>susp-exec path=</match>
    <description>Process executing from a world-writable path — possible dropped C2 implant or malware</description>
    <mitre>
      <id>T1204.002</id>
      <id>T1059.004</id>
      <id>T1071.001</id>
    </mitre>
    <group>c2,attack,</group>
  </rule>

</group>

<!--
  ============================================================================
  Threat-intelligence enrichment — CDB blocklist lookup on NSM alerts.
  Every Suricata alert (Wazuh rule 86601, groups ids/suricata) carries decoded
  `src_ip`/`dest_ip` fields. These children look them up against the
  `threat-intel-ips` CDB list (etc/lists/threat-intel-ips, registered in
  ossec.conf <ruleset>; ~735 high-confidence malicious IPs from the ipsum feed
  + the lab Sliver C2 IP) with lookup="address_match_key" (CIDR-aware), and
  escalate any hit to level 12. This turns "an alert fired" into "an alert fired
  AND the peer IP is on a known-malicious blocklist" — context, not just a match.
  The lab C2 IP (10.10.40.119) is in the list so the Phase 8 beacon traffic
  produces a threat-intel hit end-to-end.
  ============================================================================
-->
<group name="local,threat_intel,">

  <rule id="100210" level="12">
    <if_sid>86601</if_sid>
    <list field="dest_ip" lookup="address_match_key">etc/lists/threat-intel-ips</list>
    <description>Threat intel: NSM alert to a known-malicious destination IP (blocklist match) — $(dest_ip)</description>
    <mitre>
      <id>T1071</id>
    </mitre>
    <group>threat_intel,attack,</group>
  </rule>

  <rule id="100211" level="12">
    <if_sid>86601</if_sid>
    <list field="src_ip" lookup="address_match_key">etc/lists/threat-intel-ips</list>
    <description>Threat intel: NSM alert from a known-malicious source IP (blocklist match) — $(src_ip)</description>
    <mitre>
      <id>T1071</id>
    </mitre>
    <group>threat_intel,attack,</group>
  </rule>

</group>

<!--
  ============================================================================
  MISP integration — the professional TIP as the live IOC engine behind Wazuh.
  integratord runs custom-misp.py on matching alerts; it queries the MISP API for
  the alert's IOCs (IPs/hashes) and, on a hit, injects `1:misp:{...}` back into
  analysisd (decoded by the `misp` decoder). These rules fire on that injected
  event: 100301 = a known IOC matched (level 12), 100302 = integration error.
  This supersedes the static CDB list (rules 100210/100211) with a real platform
  that correlates + shares intel. See phase-7-automation/provisioning/misp-01/.
  ============================================================================
-->
<group name="misp,threat_intel,">

  <!-- The injected `1:misp:{...}` event is valid JSON, so Wazuh's built-in json
       decoder flattens it (integration, misp.value, misp.source.*). Key on that,
       not a custom decoded_as. -->
  <rule id="100300" level="3">
    <decoded_as>json</decoded_as>
    <field name="integration">^misp$</field>
    <description>MISP integration event</description>
    <options>no_full_log</options>
  </rule>

  <rule id="100301" level="12">
    <if_sid>100300</if_sid>
    <field name="misp.value">\.+</field>
    <description>MISP: known IoC $(misp.value) ($(misp.category)) matched in the threat-intel platform — triggered by rule $(misp.source.rule)</description>
    <mitre>
      <id>T1071</id>
    </mitre>
    <group>threat_intel,attack,</group>
  </rule>

  <rule id="100302" level="5">
    <if_sid>100300</if_sid>
    <field name="misp.error">\.+</field>
    <description>MISP: integration error — $(misp.error)</description>
    <group>threat_intel,</group>
  </rule>

</group>

<!--
  ============================================================================
  DNS threat-intel — actionable use of the Sysmon DNS telemetry (EID22) that the
  SwiftOnSecurity config added (2026-08-31). Wazuh decodes EID22 into
  win.eventdata.queryName (group sysmon_event_22, base rule level 0 = not
  alerted). This child does a fast CDB lookup of the queried name against the
  malicious-domains list (~1,190 ThreatFox domains + a lab test domain) and
  escalates a hit to level 12 — the domain analogue of the IP list (100210/211),
  done with a CDB lookup (not a per-query API call) because DNS is high-volume.
  NB: match_key is exact, so a subdomain of a listed domain won't match — a known
  limitation (Wazuh has no suffix-match lookup); documented, not hidden.
  ============================================================================
-->
<group name="local,windows,sysmon,dns,threat_intel,">

  <rule id="100310" level="12">
    <if_group>sysmon_event_22</if_group>
    <list field="win.eventdata.queryName" lookup="match_key">etc/lists/malicious-domains</list>
    <description>DNS query to a known-malicious domain — $(win.eventdata.queryName)</description>
    <mitre>
      <id>T1071.004</id>
    </mitre>
    <group>threat_intel,attack,dns,</group>
  </rule>

</group>

<!--
  ============================================================================
  DMZ web-attack detection — T1190 Exploit Public-Facing Application. The DMZ
  runs OWASP Juice Shop (dmz-01:3000). rtr-01's inline Suricata detects the
  attack on the wire (custom sigs 9100020-9100024 — ET web rules key on
  $HTTP_PORTS and miss 3000) and its eve.json feeds Wazuh, surfacing as the stock
  Suricata alert (rule 86601, groups ids/suricata). These children turn that into
  a labeled, MITRE-mapped SIEM detection scoped to the DMZ app — the piece the lab
  was missing: Juice Shop had attack surface + container-log ingestion but no
  detection firing on either.
  Field note: `alert.signature` is Wazuh's decoded Suricata signature text — confirmed correct via
  wazuh-logtest and the interpolated $(alert.signature) text in live 86601 alerts (2026-09-07), so that part
  was never the problem.
  Field note 2 (2026-09-07, first live fire): 100440/100441 sat at level 10/8, BELOW rule 100211 (level 12,
  "NSM alert from a known-malicious source IP" — the threat-intel CDB blocklist rule, also `if_sid=86601`).
  atk-01 (10.10.40.119) is itself in that CDB list from unrelated exercises, so EVERY attack launched from
  atk-01 against the DMZ made both 100211 and 100440 independently true on the same decoded event — and
  Wazuh's one-rule-per-event precedence (highest level among matching siblings wins) picked 100211 every
  time, so the T1190-specific alert never surfaced even though the Suricata signature and the Wazuh field
  match were both firing correctly underneath it. Confirmed via wazuh-logtest: fed the exact eve.json alert
  line through it and got rule 100211 back, never 100440. Same class of gotcha as the Sigma detections
  needing to sit above the stock L3 discovery band to win precedence — just against a different competing
  rule this time. Fixed by raising both above 100211's level, so a labeled T1190 alert always wins over a
  generic "known-bad source IP" hit when both are true; 100442 (the burst correlation) already sat at L12
  and is unaffected, since `if_matched_sid` correlation isn't subject to this same-event precedence rule.
  See phase-5-offense/attack-detect-writeups/03-web-attack-juiceshop-t1190.md.
  ============================================================================
-->
<group name="local,ids,suricata,web,attack,">

  <rule id="100440" level="13">
    <if_sid>86601</if_sid>
    <field name="alert.signature" type="pcre2">LAB WEB ATTACK (SQLi|XSS|path traversal)</field>
    <description>DMZ web attack against Juice Shop — $(alert.signature)</description>
    <mitre>
      <id>T1190</id>
    </mitre>
    <group>web_attack,attack,</group>
  </rule>

  <rule id="100441" level="13">
    <if_sid>86601</if_sid>
    <field name="alert.signature" type="pcre2">automated scanner User-Agent</field>
    <description>DMZ web app being scanned by automated tooling — $(alert.signature)</description>
    <mitre>
      <id>T1595.002</id>
    </mitre>
    <group>recon,attack,</group>
  </rule>

  <!-- Correlation: a burst of web-attack signatures from one source = an active
       exploitation run (sqlmap/nikto), not a stray probe. Escalates to level 12. -->
  <rule id="100442" level="12" frequency="8" timeframe="60">
    <if_matched_sid>100440</if_matched_sid>
    <same_field>src_ip</same_field>
    <description>DMZ web application under active attack — burst of web-attack signatures from $(src_ip) (T1190)</description>
    <mitre>
      <id>T1190</id>
    </mitre>
    <group>web_attack,attack,</group>
  </rule>

  <!-- DNS tunneling — same pattern as 100440 (T1190): rtr-01's inline Suricata
       already alerts on this at the wire (router role, sid 9100010, "LAB DNS
       tunneling..." signature) and reaches Wazuh as a stock 86601 Suricata
       alert, but until now nothing gave it a labeled, MITRE-mapped rule the
       way the web attack got. GAP FOUND running the capstone for real
       (2026-09-07): a live iodine tunnel fs-01->atk-01 (60 pings, 0% loss,
       Suricata sid 9100010 fired 126x in eve.json on rtr-01) never once
       produced a "DNS tunneling" alert in Wazuh — it only showed up as the
       generic threat-intel CDB hit (rule 100210, dest_ip match), and only
       because atk-01's IP happens to be blocklisted from unrelated exercises.
       Remove that coincidence and this technique would have been fully
       invisible to the SIEM, caught only in Suricata's own eve.json on the
       sensor host — a real SIEM-integration gap, not a documentation one.
       Fixed the same way 100440 was fixed: a child of 86601 keyed on the
       Suricata signature text, at level 13 so it always wins the same-event
       precedence tie against 100210/100211 (both level 12) — see the T1190
       field note above for why an equal-or-lower level loses that race. -->
  <rule id="100443" level="13">
    <if_sid>86601</if_sid>
    <field name="alert.signature" type="pcre2">LAB DNS tunneling</field>
    <description>DNS tunneling to REDTEAM — long encoded query names ($(alert.signature))</description>
    <mitre>
      <id>T1048.003</id>
      <id>T1071.004</id>
    </mitre>
    <group>exfiltration,attack,</group>
  </rule>

</group>

<!--
  ============================================================================
  YARA-on-FIM — malware detection on the fs-01 share. Two-stage:
  (1) 100450 fires on any file added/modified under the share path (a syscheck
      child) and is the trigger the `yara` active-response keys on (see the siem
      role ossec.conf command/active-response) — it runs yara.sh on that agent
      against the changed file. Level 3: the write itself is routine, it's just
      the dispatch point.
  (2) 100460 fires on a YARA match reported back via active-responses.log
      (decoded by wazuh-yara / wazuh-yara-result) — malware signature matched a
      file on the share. 100461 surfaces scanner errors so a broken loop is loud,
      not silent ([[Silent Fallbacks]]).
  Field note: verify the syscheck path field with wazuh-logtest on first fire
  (Wazuh exposes the changed path as `file`); the AR trigger only needs 100450 to
  fire, which the if_group + path match guarantees.
  ============================================================================
-->
<group name="local,yara,malware,">

  <rule id="100450" level="3">
    <if_group>syscheck</if_group>
    <field name="file">^/srv/samba/public</field>
    <description>File added/modified in the fs-01 share — dispatching YARA scan</description>
    <group>syscheck,yara,</group>
  </rule>

  <rule id="100460" level="12">
    <decoded_as>wazuh-yara</decoded_as>
    <field name="yara_rule">\.+</field>
    <description>YARA malware match: $(yara_rule) on the fs-01 share — $(yara_scanned_file)</description>
    <mitre>
      <id>T1204.002</id>
    </mitre>
    <group>yara,malware,attack,</group>
  </rule>

  <rule id="100461" level="5">
    <decoded_as>wazuh-yara</decoded_as>
    <match>wazuh-yara: ERROR</match>
    <description>YARA active-response error — malware scan did not run correctly</description>
    <group>yara,</group>
  </rule>

</group>
